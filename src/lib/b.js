/**
 * @param {number} a
 * @param {number} b
 */
function sum(a, b) {
  return a + b;
}

/**
 * @param {number} a
 * @param {number} b
 * @param {number} c
 */
function sumWithThreeParam(a, b, c) {
  return a + b + c;
}

export {
  sum,
  sumWithThreeParam,
};
